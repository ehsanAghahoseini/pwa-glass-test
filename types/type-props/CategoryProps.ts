import { CategoryModel } from "../models/category.types"
import { FilterItemsModel } from "../models/filterItems.types"

export declare type CategoryProps = {
    categoryData: CategoryModel[],
    filterFailed:boolean ,
}